{
  "name": "alpinejs-playground",
  "private": true,
  "version": "1.0.0",
  "description": "A set of ready to use Alpine.js examples with TailwindCSS",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && node scripts/build-index.js && cp pages/* dist && node scripts/inline-remote-css/cli.js",
    "start": "serve pages",
    "format": "prettier ./pages/* README.md scripts/* --write"
  },
  "keywords": [
    "alpinejs",
    "tailwindcss"
  ],
  "author": "Hugo Di Francesco",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "@11ty/eleventy": "^0.10.0",
    "node-fetch": "^2.6.0",
    "prettier": "^1.19.1",
    "purgecss": "^2.1.0",
    "serve": "^11.3.0"
  }
}
